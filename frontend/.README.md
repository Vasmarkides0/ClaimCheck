Frontend

The user interface for ClaimCheck was built using Lovable during the UCL AI Festival Hackathon.

The Lovable interface connects to the FastAPI backend via REST endpoints to:
- upload pitch decks
- trigger claim extraction and verification
- display the credibility scoring report

The frontend code was generated using Lovable's platform and is not included in this repository.